all:
  hosts:
    slave-node:
      ansible_host: ansible-slave
      ansible_user: ansible
      # פקודה זו תמשוך את המשתנה מהסביבה של ה-Master
      ansible_password: "{{ lookup('env', 'ANSIBLE_PASSWORD') }}"
      ansible_python_interpreter: /usr/bin/python3