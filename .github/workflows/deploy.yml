name: Build and Push Latest to Docker Hub

on:
  push:
    branches:
      - master

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the code
      uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        # 1. אומרים לדוקר שהקוד וה-Dockerfile נמצאים בתוך התיקייה הזו
        context: ./CI-CD/github-action
        # 2. הכתובת המדויקת לקובץ ה-Dockerfile
        file: ./CI-CD/github-action/Dockerfile
        push: true
        tags: eli00504167101/app-node:latest
